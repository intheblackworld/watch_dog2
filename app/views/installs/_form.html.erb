<%= form_for(@install) do |f| %>
  <% if @install.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@install.errors.count, "error") %> prohibited this install from being saved:</h2>

      <ul>
      <% @install.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="col-md-4 col-md-offset-4 down-mid">
    <h1>新增安裝案例</h1>
    <div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_area :description, class: 'form-control', placeholder: '輸入敘述' %>
  </div>
    <div class="form-group">
    <%= f.label :area %><br>
    <%= f.text_field :area, class: 'form-control', placeholder: '輸入地區' %>
  </div>
    <div class="form-group">
    <%= f.label :image %><br>
    <%= f.file_field :image, class: 'form-control' %>
    </div>
  <div class="row">
  <div class="col-md-4">
  <div class="actions">
    <%= f.button :submit, class: 'btn btn-primary btn-lg' %>
  </div>
  <hr>
  <%= link_to '回上頁', installs_path %>
  </div>
  </div>
  </div>
<% end %>

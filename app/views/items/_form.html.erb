
<%= form_for [@classification,@item] do |f| %>

  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="col-md-4 col-md-offset-4 down-mid">
    <h1>新增商品</h1>
    <div class="form-group">
    <%= f.label :model_number %><br>
    <%= f.text_field :model_number, class: 'form-control', placeholder: '輸入型號' %>
  </div>
    <div class="form-group">
    <%= f.label :title %><br>
    <%= f.text_field :title, class: 'form-control', placeholder: '輸入名稱' %>
  </div>

    <div class="form-group">
    <%= f.label :standard %><br>
    <%= f.text_area :standard, class: 'form-control', placeholder: '輸入規格' %>
  </div>
    <div class="form-group">
    <%= f.label :image %><br>
    <%= f.file_field :image, class: 'form-control' %>
    </div>
  <div class="row">
  <div class="col-md-4">
  <div class="actions">
    <%= f.button :submit, class: 'btn btn-primary btn-lg' %>
  </div>
  <hr>
  <%= link_to '回上頁', :back %>
  </div>
  </div>
  </div>
<% end %>

<%= render 'addnew'%>

<div class="container">

        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">產品類別
                    <small></small>
                </h1>
            </div>
        </div>



    <% @classifications.each_slice(4) do |classifications| %>
      <div class="row">
      <% classifications.each do |classification| %>
      <div class="col-md-3 center-block">
        <%= link_to "#{classification.title}", classification_items_path(classification), class: "btn btn-success" %>
        
        <% if !current_user %>
        <%else%>
        <%= link_to 'Edit', edit_classification_path(classification) %>
        <%= link_to 'Destroy', classification, method: :delete, data: { confirm: 'Are you sure?' } %>
        <%end%>
        <hr>
       </div>
       <%end%> 
      </div>

    <% end %>



</div>

   <!-- Pagination -->
        <div class="row text-center">
            

          <%= link_to '回到首頁', root_path, class: "btn btn-info btn-lg" %>

        </div>
        <!-- /.row -->
      

        <hr>


        <!-- Footer -->
        
      <!--聯絡我們 -->



